# Backlog

## Deferred

- Add an archive build/version indicator and or cache-busting flow for `archive.sqlite` in local browser development. Rebuilding the database while the dev server or browser still serves older bytes can make people/person validation look broken even when the route and query code are correct.
